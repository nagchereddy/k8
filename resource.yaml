apiVersion: v1
kind: LimitRange
metadata:
  - name: Default-memory
spec:
  limits:
    - default:
        memory: 512Mi
      defaultRequest:
        memory: 256Mi
      type: Container 

    